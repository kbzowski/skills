# radashi number

12 functions. Import: `import { ... } from 'radashi'`.

| function | description |
| --- | --- |
| `clamp` | Limit the range of a variable number |
| `inRange` | Verifies number within range, inclusive start, exclusive end, flexible order, defaulting to 0 if end unspecified |
| `lerp` | Smoothly transitions between two values based on a factor |
| `max` | Get the largest item from an array |
| `min` | Get the smallest item from an array |
| `parseDuration` | Parses a duration string into milliseconds |
| `parseQuantity` | Parses a quantity string into its numeric value |
| `range` | Create a range used for iterating |
| `round` | Rounds a number to a specified precision. |
| `sum` | Add up all items of an array |
| `toFloat` | Convert a value to a float if possible |
| `toInt` | Convert a value to an int if possible |

