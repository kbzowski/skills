# radashi function

5 functions. Import: `import { ... } from 'radashi'`.

| function | description |
| --- | --- |
| `always` | Create a function that always returns the same value |
| `castComparator` | Cast a value into a comparator function |
| `castMapping` | Cast a value into a mapping function |
| `identity` | Returns the value passed to it |
| `noop` | Does nothing and returns undefined |

