# radashi typed

34 functions. Import: `import { ... } from 'radashi'`.

| function | description |
| --- | --- |
| `assert` | Asserts a condition and narrows the type of the value |
| `getErrorMessage` | 'Get a readable message from an unknown error value' |
| `isArray` | 'Determine if a value is an Array' |
| `isAsyncIterable` | Determine if a value is an async iterable |
| `isBigInt` | 'Determine if a value is a BigInt' |
| `isBoolean` | Check if a value is a boolean type |
| `isClass` | Determine if a value was declared with `class` syntax |
| `isDate` | 'Determine if a value is a Date' |
| `isEmpty` | 'Determine if a value is empty' |
| `isEqual` | Determine if two values are equal |
| `isError` | 'Determine if a value is an Error' |
| `isFloat` | 'Determine if a value is a float' |
| `isFunction` | 'Determine if a value is a Function' |
| `isInt` | 'Determine if a value is an int' |
| `isIntString` | 'Determine if a value is an int in string form' |
| `isMap` | Returns true for Map instances |
| `isMapEqual` | Compare two maps for deep equality |
| `isNullish` | Determine if a value is null or undefined |
| `isNumber` | 'Determine if a value is a number' |
| `isObject` | 'Determine if a value is an Object' |
| `isPlainObject` | 'Determine if a value is a plain object' |
| `isPrimitive` | Checks if the given value is primitive |
| `isPromise` | 'Determine if a value is a Promise or has a `then` method' |
| `isRegExp` | Returns true for RegExp instances |
| `isResult` | Check if a value is a Result tuple |
| `isResultErr` | Returns true for failed Result tuple |
| `isResultOk` | Returns true for successful Result tuple |
| `isSet` | Returns true for Set instances |
| `isSetEqual` | Compare two sets for shallow equality |
| `isString` | 'Determine if a value is a String' |
| `isSymbol` | 'Determine if a value is a Symbol' |
| `isUndefined` | 'Determine if a value is undefined' |
| `isWeakMap` | Returns true for WeakMap instances |
| `isWeakSet` | Returns true for WeakSet instances |

