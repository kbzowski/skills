# radashi array

35 functions. Import: `import { ... } from 'radashi'`.

| function | description |
| --- | --- |
| `alphabetical` | Sorts an array of objects alphabetically by a property |
| `boil` | 'Reduce a list of items down to one item' |
| `cartesianProduct` | Perform a Cartesian product of arrays |
| `castArray` | Cast a value into an array |
| `castArrayIfExists` | Cast a non-nullish value into an array |
| `cluster` | Split a list into many lists of the given size |
| `concat` | Flattens and filters nullish values from arguments |
| `counting` | Creates an object with counts of occurrences of items |
| `diff` | Create an array of differences between two arrays |
| `first` | Get the first item from a list |
| `flat` | Flatten an array of arrays into a single dimension |
| `fork` | Split an array into two arrays by a condition |
| `group` | Categorize elements from an array into distinct groups |
| `intersects` | Determine if two arrays have a common item |
| `isArrayEqual` | Checks if two arrays are equal in length and content |
| `iterate` | Iterate over a callback n times |
| `last` | Get the last item from a list |
| `list` | Create a list with specific items |
| `mapify` | Convert an array to a map |
| `merge` | Combine two lists overriding items in the first |
| `objectify` | Convert a list to a dictionary object |
| `pluck` | Map an array of objects to an array of arrays |
| `remove` | 'Filter an array by removing elements that satisfy a predicate function' |
| `replace` | Replace an item in an array |
| `replaceOrAppend` | Replace item in array or append if no match |
| `select` | Filter and map an array |
| `selectFirst` | Find and map the first array element meeting a condition |
| `shift` | Shift array items by n steps |
| `sift` | Remove all falsy items from list |
| `sort` | Sort a list of items by a numerical property or value |
| `toggle` | Toggles an item's existence in an array |
| `unique` | Remove duplicates from an array |
| `unzip` | Group array elements by their index position across the input arrays |
| `zip` | Combine multiple arrays in sets |
| `zipToObject` | Combine multiple arrays in sets |

